import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-quiz-game',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
